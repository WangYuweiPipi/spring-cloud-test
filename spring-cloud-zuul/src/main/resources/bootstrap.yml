spring:
  application:
    name: cloud-zuul
server:
  port: 8004

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
zuul:
  routes:
    user:
      path: /eureka-client/**
      serviceId: eureka-client
